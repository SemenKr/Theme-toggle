extends ./pug/components/base/layout

block variables
    - const title = "Theme-Toggle" //- на страницах кроме главной используйте О нас — ProjectName
    // - const wrapperMods = ""
    // - const hasHeader = true
    // - const headerMods = "no-logo-desktop"
    // - const headerType = "default"
    // - const hasFooter = true
    // - const footerMods = ""
    // - const currentUrl = "index.html" //- для остальных страниц пишем пустую строку или индекс активного headerLinks

block data

block main
    main.page
        .sky
            .body


block modals

